# 1.2.1 收尾优化 — 开工指令（DSH 出单，Trae 实现）

> 来源：`README.md`「路线图 → 收尾优化」4 条未完成待办（已在记忆轨召回确认）。
> 角色分工：**DSH（D 侧）= 需求 + 设计 + 评审 + 最终验收**，只出设计单/开工指令，不改生产代码；
> **Trae = 生产方**，负责实现代码。流程：D 出单 → Trae 实现 → D 读代码核验 + 跑 pytest → 最终验收。
>
> 单号编号：`KL-1.2.1-01` ~ `04`；`归属` 标注由谁实现（DSH 自己 / Trae）。

| 单号 | 优先级 | 待办 | 归属 |
| --- | --- | --- | --- |
| KL-1.2.1-01 | 低 | 统一硬编码版本号 | Trae |
| KL-1.2.1-02 | 高 | 安装版静默升级未生效 | Trae |
| KL-1.2.1-03 | 中 | 记忆 SKILL 内联/收尾复盘未自动触发 | DSH + Trae（分轨） |
| KL-1.2.1-04 | 中 | 托盘退出未杀 DSH 进程 | Trae |

---

## KL-1.2.1-01 统一硬编码版本号（优先级：低）

### 问题背景
`/api` 首页接口与 MCP server meta 仍返回旧品牌 / 旧版本 `1.0.0` / `SSEditor`，未随 1.2.0 更新。
现仓库版本：`VERSION` = `1.2.0`，`app_version.txt` = `1.2.0.002`（构建号）。

### 根因定位（文件:行）
- `src/app.py` 第 55-61 行 `api_index()`：返回 `{'name': 'SSEditor', 'version': '1.0.0', ...}`。
- `src/modules/mcp/routes.py` 第 33 行：`MCP_SERVER_VERSION = '1.0.0'`（第 172 行引用它返回给 MCP meta `version`）。
- `src/modules/shared/templates/shared.html` 第 75 行：`<input id="sh-pub-version" ... placeholder="1.0.0" ...>`。

### 设计改动（Trae 实现）
1. **单一版本常量**（防止再次漂移，作为统一来源）：
   - 在 `src/config.py` 的 `Config` 之上新增模块级常量：
     - `APP_VERSION = '1.2.0'`（主版本，对应 `VERSION` 文件内容；不取构建号 app_version.txt 的 4 段值，/api 与 MCP meta 暴露主版本即可）。
     - `APP_NAME = 'PersonLLMWiki'`。
2. `src/app.py` `api_index()`：`name` 改 `APP_NAME`，`version` 改 `APP_VERSION`（`from config import APP_NAME, APP_VERSION`）。
3. `src/modules/mcp/routes.py`：`MCP_SERVER_VERSION = APP_VERSION`（`from config import APP_VERSION`），删除字面量 `'1.0.0'`。
4. `src/modules/shared/templates/shared.html` 第 75 行：`placeholder` 改 `1.2.0`（HTML 模板无法直接引 Python 常量，字面量更新即可；如评估后仍希望服务端注入，可由后端 render 传变量，但以最小改动优先）。

### 范围说明（非本次改动）
- `sseditor.db`（`config.py` 第 55 行数据库文件名）与 `.env` 里 legacy token（`ZSSNOTE_MCP_TOKEN`）为**内部历史命名**，非用户可见品牌/版本，**不改**，避免存量数据迁移风险。
- 仅全局搜 `SSEditor|'1.0.0'|"1.0.0"` 于 `src/` 确认无其它待改点（已知结果即上表 3 处 + `mcp/routes.py` 引用）。

### 验收标准
- `GET /api` 返回 `name = "PersonLLMWiki"`、`version = "1.2.0"`。
- MCP `meta`（`mcp/routes.py` / MCP 标准 `serverInfo`）返回 `version = "1.2.0"`。
- `shared.html` 版本输入框 placeholder 显示 `1.2.0`。
- 全量 `pytest` 通过；无新增 `1.0.0` / `SSEditor` 用户可见残留。

---

## KL-1.2.1-02 安装版静默升级未生效（优先级：高）

### 问题背景
实测「下载并升级」后，安装目录下 `app_version.txt` 仍为旧构建号、exe 时间戳仍为旧构建（如 08-27），即静默安装**未真正替换运行中的应用**。`C:\Program Files\PersonLLMWiki` 也不存在，说明应用运行路径与安装器目标路径不一致。

### 根因定位（文件:行）
- `packaging/installer.iss` 第 16 行：`DefaultDirName={pf}\PersonLLMWiki` —— 安装器默认装到 `Program Files`；而实际运行中的应用可能被安装在自定义目录（如 `D:\PersonLLMWiki`），故升级装到了「另一个目录」的新副本，**不会替换运行中的应用**。
- `src/common/upgrade_installer.py` 第 23-25 行 `build_silent_args()`：只拼 `SILENT_ARGS`（`/VERYSILENT /SUPPRESSMSGBOXES /NORESTART`），**未加 `/DIR` 指向当前安装目录**。
- `src/common/upgrade_installer.py` 第 20 行 `_WAIT_BEFORE_INSTALL_MS=3000`：固定等待；若应用尚未完全释放 exe，安装器替换失败。
- 提权链路：`installer.iss` 第 23 行 `PrivilegesRequired=admin` + `wscript.exe` 非提权启动 + `/VERYSILENT` 静默 → 无法弹 UAC，静默安装可能直接失败或未真正落盘。

### 设计改动（Trae 实现）
1. **安装目标指向当前运行目录**：
   - 在 `build_silent_args()` 中，当打包态（`sys.frozen`）时显式追加 `/DIR=<安装目录>`；安装目录取 `os.path.dirname(sys.executable)`（exe 所在目录）或 `_internal` 所在目录的父级。
   - `installer.iss` 增补 `UsePreviousAppDir=yes`，保证静默升级复用上次安装目录；同时保留 `/DIR` 兜底，双保险。
2. **提权策略**（让静默安装真正以管理员权限执行并替换 Program Files 下的应用）：
   - 方案 A：watcher（`spawn_watcher` 内 VBS）改用 `ShellExecute "runas"` 拉起安装器，使 setup 继承提权；给 setup 足够的静默参数。
   - 方案 B（若方案 A 不可行/复杂）：安装器改为不强制 admin，或设置 `PrivilegesRequiredOverridesAllowed=dialog`，使非提权下仍可静默完成对当前用户目录的替换。
   - 无论哪套方案，需验证 `/VERYSILENT` 下安装器确实落盘到 `/DIR` 指定目录。
3. **退出/替换时序**：
   - 将固定 `_WAIT_BEFORE_INSTALL_MS` 替换为「等运行中应用进程完全退出 + exe 可被替换」的轮询等待（带上限，如 30s；超时再走 `/CLOSEAPPLICATIONS` 兜底，`installer.iss` `[Setup]` 增补 `CloseApplications=yes`）。
   - 确认 `settings/routes.py` 第 729-744 行 `_do_launch` 的退出路径在 spawn watcher 后确实让主进程释放 exe 后再交给安装器。
4. **回归测试**：
   - `tests/desktop/` 新增 `test_upgrade_installer.py`：mock `sys.frozen`，断言 `build_silent_args()` 输出含 `/DIR=<安装目录>`；断言 watcher VBS 包含等待/提权逻辑。
   - 现有测试 `tests/desktop/test_dsh_bridge.py` 等复用不变。

### 验收标准
- 真实安装态（如已装在 `D:\PersonLLMWiki`），在版本门禁用例下点「下载并升级」：应用退出 → 静默安装真正替换**同目录**应用 → 自动重启。
- 安装后：`app_version.txt` 更新为新构建号；exe 时间戳更新到本次构建；`C:\Program Files` **不**出现副本（因为装到了当前目录）。
- 开发态（`_is_dev_mode`）强制 dry-run 不回归：仍只返回参数、不安装不退出。

---

## KL-1.2.1-03 记忆 SKILL 内联/收尾复盘未自动触发（优先级：中）

### 问题背景
实测 DSH 对话中只有用户**明确说「记住」**才写入记忆；记忆 SKILL（`~/.dsh/skills/memory`）里的「过程中内联记」「收尾复盘」**未自动触发**，纯靠 agent 自觉 `remember` 不可靠。
README 给出两方向：**DSH 侧会话结束钩子 / workflow 触发复盘**，或 **PLW 侧「多信号沉降」空闲超时 / 批量沉降通道**。

### 角色分工（本单拆两轨）
- **轨道 A（DSH 自己负责，不派 Trae）**：在 DSH 侧把记忆 SKILL 的「收尾复盘」从 agent 自觉改为**必做契约**——三段式必做（开场召回 / 过程中内联记 / 收尾复盘）+ 明确收束信号触发 + 逐条查重去重 + 接入批量沉降通道。落地于 `~/.dsh/skills/memory/SKILL.md`。
- **轨道 B（Trae 生产实现）**：在 PLW 记忆模块落地「多信号沉降」通道，作为不依赖 agent 自觉的自动化兜底。

### 设计改动（轨道 B，Trae 实现）
参考 `src/modules/memory/`（`storage.py` / `retrieval.py` / `routes.py`）与 `resource/memories/*.md`（+ `_raw/`）：
1. **批量沉降接口**：新增 MCP 工具 / API（如 `settle_memory`），接受 `[{body, kind, source}]` 列表，一次原子写入多条到 `memories`；写入前对每条做**查重**（复用 `search_memory` / embedding+BM25，避免与既有记忆近似重复）。
2. **空闲超时沉降**：在 `src/common/scheduler.py`（或会话级 tracker）增加一个空闲检测：进程级 hook 采集的原始 trace 先入 `_raw/` 暂存，会话空闲超过阈值（如 N 分钟）后，把成型项批量沉降进 `memories`。
3. **双轨制约束**：沉降产物走**自动记忆**轨（低门槛入库 + 一键撤回 + 带来源），**不混入**人工审批知识轨；绝不自动 `forget_memory`（撤回权归用户）。
4. **去重**：沉降前对每条做近似去重（与已有记忆比较），宁缺毋滥。

### 验收标准（轨道 B）
- 一次 agent 会话结束、空闲期过后，暂存的成型记忆项**自动**沉降入 `memories/*.md`，无需用户显式「记住」。
- 沉降不产生重复条目；不自动撤回任何记忆。
- 现有记忆/知识双轨制测试与全量 `pytest` 通过。

### 备注
- 轨道 A（DSH 侧记忆 SKILL 复盘契约）已由 DSH 落地：`~/.dsh/skills/memory/SKILL.md` 改为三段式必做 + 收束信号 + 查重去重 + 批量沉降通道（`settle_memory` 可用时优先，否则逐条 `remember`）。
- 残余说明：本机 DSH 为 npm 安装的 `@deepseek-ai/dsh`（`lib/` 为打包产物），未在该 checkout 暴露可编辑的 harness 会话结束钩子源码；故采用「SKILL 必做契约」而非改 harness 源码。**「纯自动（零 agent）接线」已出单并实现，见 KL-1.2.1-05**：新增 `stage_memory_signals` 暂存入口（卫生过滤）+ `settle_memory_signals` 空闲沉降，二者都汇入 `settle_memories()`（查重、auto+source、绝不自动撤回）。

---

## KL-1.2.1-04 托盘退出未杀 DSH 进程（优先级：中）

### 问题背景
PLW 桌面壳托盘「退出」时，DSH（3080）进程未一并终止、残留占用端口。要求：**只停 PLW 拉起的 DSH，不误杀用户自启的 DSH**。

### 根因定位（文件:行）
- `src/desktop.pyw` 第 361-367 行 `_quit_app()`（托盘「退出」回调 + `register_quit` 的升级退出回调）：只 `_save_window_state()` + `_allow_close[0]=True` + `PostMessageW(WM_CLOSE)`，**未调用 `dsh_bridge.stop()`**。
- `src/common/dsh_bridge.py` 第 498-528 行 `stop()` 已经实现「只杀 `_managed_proc`（本进程拉起的 DSH），非本进程拉起的 DSH 不强制 kill」——正是所需语义，仅缺一处调用。

### 设计改动（Trae 实现）
1. 在 `src/desktop.pyw` `_quit_app()` 里，于 `PostMessageW(WM_CLOSE, ...)` **之前**调用 `dsh_bridge.stop()`（用 `try/except` 包裹，永不阻塞退出；`dsh_bridge` 已按 `_managed_proc` 判定，只杀 PLW 拉起的 DSH）。
2. 兜底：在 `webview.start()` 返回后的收尾（`desktop.pyw` 第 740 行附近，`tray.stop()` 前）再调一次 `dsh_bridge.stop()`（幂等），覆盖其它退出路径；可选在 `main()` 外层 `finally` 兜底。
3. 注意：`hide-to-tray` 关闭（`_wndproc` 里 `_allow_close[0]` 为 False 时 `WM_CLOSE → SW_HIDE`，`desktop.pyw` 第 282-284 行）**不应**杀 DSH —— 只在真正退出路径调用。
4. **回归测试**：`tests/desktop/test_dsh_bridge.py` 新增用例：mock `_managed_proc` 存活时 `stop()` 以 `taskkill /PID /T /F` 终止之；`_managed_proc` 为 None（用户自启 DSH）时 `stop()` 不执行 taskkill、不误杀。

### 验收标准
- 托盘「退出」后，PLW 关闭且 **PLW 拉起的 DSH 进程一并终止**（3080 端口释放）。
- 用户自行启动（非 PLW 拉起）的 DSH 不被杀。
- 点窗体「✕」隐藏到托盘**不**杀 DSH；仅真正退出时杀。

---

## 交付清单（DSH 后续核验）
1. `src/config.py` 新增 `APP_VERSION` / `APP_NAME`；`src/app.py`、`src/modules/mcp/routes.py` 引用之；`src/modules/shared/templates/shared.html` placeholder 更新。
2. `src/common/upgrade_installer.py`（`build_silent_args` 加 `/DIR`、提权、等待时序）+ `packaging/installer.iss`（`UsePreviousAppDir` / `CloseApplications`）+ 新增 `tests/desktop/test_upgrade_installer.py`。
3. `src/modules/memory/`（批量沉降 + 空闲超时）+ `src/common/scheduler.py`（轨道 B）；轨道 A（DSH 侧记忆 SKILL 复盘契约）已由 DSH 落地至 `~/.dsh/skills/memory/SKILL.md`。
4. `src/desktop.pyw`（`_quit_app` 收尾调用 `dsh_bridge.stop()`）+ `tests/desktop/test_dsh_bridge.py` 新增用例。
5. 【追加-KL-1.2.1-05】记忆「多信号沉降」自动接线：`src/modules/memory/settle.py` 新增 `stage_memory_signals()`（对 `stage_raw_trace` 的增强入口，内建卫生过滤）；`src/modules/mcp/tools_memory.py` + `tools_registration.py` 注册 MCP 工具 `stage_memory_signals`；`tests/memory/test_settle.py` 增补用例。

---

## KL-1.2.1-05 记忆「多信号沉降」自动接线（优先级：中，Trae 生产实现）

### 目标
把记忆「写盘/沉降」这步做成**全自动**：候选信号被暂存进 `_raw/pending.jsonl` 后，由既有空闲任务 `settle_if_idle()` 自动沉降（查重、写 `status=auto`、带 `source`、绝不自动撤回）。补一个可调用的**暂存入口** + 卫生过滤，让沉降通道真正有料可沉降。

### 信号源说明（重要）
PLW 侧**没有**「过程级会话 trace」来源（对话在 DSH 侧，`src/modules/agent/routes.py` 只是壳、`dsh_bridge` 仅做进程管理/headless）。因此本单**不**从 PLW 进程内盲采会话；信号来源是 **DSH 复盘契约**（对话提取候选）或其它明确产生方，经本单的 `stage_memory_signals` 入暂存。这样把「提取（DSH 侧）」与「写盘/沉降（PLW 侧，全自动）」解耦。

### 设计改动（Trae 实现）
1. `src/modules/memory/settle.py`：新增 `stage_memory_signals(items, source='signal')` —— 对 `stage_raw_trace()` 的增强包装，追加**卫生过滤**：
   - 每条 `body` 去空格非空、`kind ∈ {preference,fact,decision,other}`（非法归 `other` 或跳过）、`body` 长度上限（如 ≤200，超长截断或跳过）。
   - 与**已有 memories + 本批/暂存内已存在项**做轻量近似查重，命中则跳过再暂存（避免重复喂养沉降通道）。可复用 `_is_duplicate`。
   - 过滤通过后追加写入 `_raw/pending.jsonl`（复用 `stage_raw_trace` 的锁与 JSONL 格式）。
   - 返回 `{staged, skipped, total}`。
   - 语义「是否成型/值得记」不在此做脆弱启发式判断，交由沉降查重 + 用户一键撤回兜底（宁缺毋滥）。
2. `src/modules/mcp/tools_memory.py` + `tools_registration.py`：注册 MCP 工具 `stage_memory_signals`（input_schema：`{items:[{body,kind?,source?}]}`，`required:['items']`；handler 调 `stage_memory_signals`；`cost='openai-embedding'`），供 DSH 复盘契约等调用。
3. 空闲沉降链路确认（不改，仅核验）：`src/common/scheduler.py` 的 `_idle_memory_settle` → `settle_if_idle()`（`MEMORY_SETTLE_CHECK_INTERVAL_S`，默认 60s）已就绪；确认它读到的是同一 `_raw/pending.jsonl`。
4. 约束：沉降走 `settle_memories()`（查重、`status=auto`、带 source、绝不自动 forget、双轨制不混入人工审批轨）。

### 验收标准
- 调用 `stage_memory_signals` 后，合法候选写入 `_raw/pending.jsonl`；空/非法 kind/超长/近似重复被跳过。
- 空闲超时（`settle_if_idle` / scheduler job）把暂存自动沉降入 `memories/*.md`（auto + source），沉降后清空暂存；重复项不重写。
- 现有记忆/知识双轨制测试与全量 `pytest` 通过；不自动撤回任何记忆。

